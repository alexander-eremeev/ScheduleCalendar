<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Include required permissions for Google Mobile Ads to run-->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <!-- Yandex Metrica optional permission. Approximate location derived from network location sources such as cell towers and Wi-Fi -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    -->
    <!-- Yandex Metrica optional permission. Precise location from location sources such as GPS, cell towers, and Wi-Fi -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    -->
    <!-- Yandex Metrica optional permission. Wifi state: mac, ssid, ... -->
    <!--
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    -->

    <!--
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"

            android:name="com.childmathematics.android.shiftschedule.InventoryApplication"
                android:name="com.childmathematics.android.shiftschedule.MainActivity"

    -->
    <application
        android:name="com.childmathematics.android.shiftschedule.MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScheduleCalendar"
        tools:targetApi="34">

        <!-- Sample AdMob app ID: ca-app-pub-3940256099942544~3347511713
        -->
        <meta-data
        android:name="com.google.android.gms.ads.APPLICATION_ID"
        android:value="ca-app-pub-3940256099942544~3347511713"/>

        <activity
            android:name="com.childmathematics.android.shiftschedule.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ScheduleCalendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                       <intent-filter>
                           <action android:name="android.intent.action.VIEW" />
                           <category android:name="android.intent.category.DEFAULT" />
                           <category android:name="android.intent.category.BROWSABLE" />
                           <data android:scheme="file" android:host="file:///android_asset" />
                       </intent-filter>
            -->
                   </activity>
               </application>

           </manifest>